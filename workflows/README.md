# Workflows

Store UiPath workflow projects and related automation assets here.

Suggested approach:

- keep one clearly named project folder for the UiBank registration flow
- add a short README in each workflow project if the entry point is not obvious
- document any required test data or environment assumptions in `docs/` or `research/`
